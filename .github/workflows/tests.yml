name: Run Tests

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test-logic:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: make -j test
