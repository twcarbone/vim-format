" Comment
  " Indented comment
" Comment with a "string" contained
"Comment that just looks like a string"
	" Tab-indented comment with "string"
"" Comment, but with multiple (even) leaders
""" Comment, but with multiple (odd) leaders

let a = ["a", "b"] "inline comment
let a = 2"inline comment with no leading whitespace
